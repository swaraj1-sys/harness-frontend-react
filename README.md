# React Frontend (Vite)

## Local Setup
1. Install dependencies: `npm install`
2. Run development server: `npm run dev`

## Testing
Run unit tests: `npm test`

## Docker
Build: `docker build -t harness-frontend .`
Run: `docker run -p 80:80 harness-frontend`
